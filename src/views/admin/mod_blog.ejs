<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title != 'undefined' ? title : 'Admin Dashboard' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
    <div class="container pt-5 shadow-lg rounded" style="height: 100vh;">
        <h1 class="mb-4 text-center"><%= typeof blog != 'undefined' ? 'Edit Blog' : 'Add Blog' %></h1>
        <div class="col-md-8 mx-auto">
            <form action="<%= typeof blog != 'undefined' ? '/admin/blog/edit/' + blog.id : '/admin/blog/add' %>" method="POST">
                <div class="mb-3">
                    <label for="title" class="form-label">Title</label>
                    <input type="text" class="form-control" id="title" name="title" value="<%= typeof blog != 'undefined' ? blog.title : '' %>" required>
                </div>
                <div class="mb-3">
                    <label for="desc" class="form-label">Description</label>
                    <textarea class="form-control" id="desc" name="desc" rows="5" required><%= typeof blog != 'undefined' ? blog.desc : '' %></textarea>
                </div>
                <button type="submit" class="btn btn-primary"><%= typeof blog != 'undefined' ? 'Save Changes' : 'Add Blog' %></button>
                <a href="/admin" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</body>

</html>