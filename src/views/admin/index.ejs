<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !='undefined' ? title : 'Admin Dashboard' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
    <div class="container pt-5 shadow-lg rounded" style="height: 100vh;">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Welcome, <%= username %>!</h1>
            <a href="/admin/logout" class="btn btn-danger">Logout</a>
        </div>
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>All Blogs</h2>
            <a href="/admin/blog/add" class="btn btn-success" data-toggle="tooltip" data-placement="top" title="Add Blog"><i class="fa fa-plus"></i></a>
        </div> 
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th class="text-center align-middle">ID</th>
                        <th class="text-left align-middle">Title</th>
                        <th class="text-left align-middle">Description</th>
                        <th class="text-center align-middle">Date</th>
                        <th class="text-right align-middle">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (blogs.length === 0) { %>
                        <tr>
                            <td colspan="5" class="text-center">No blogs available</td>
                        </tr>
                    <% } else { %>
                        <% blogs.forEach(blog => { %>
                            <tr>
                                <td class="text-center align-middle"><%= blog.id %></td>
                                <td class="text-left align-middle"><%= blog.title %></td>
                                <td class="text-left align-middle"><%= blog.desc %></td>
                                <td class="text-center align-middle"><%= blog.date %></td>
                                <td class="text-right align-middle">
                                    <a href="/admin/blog/edit/<%= blog.id %>" class="btn btn-primary btn-sm" data-toggle="tooltip" data-placement="top" title="Edit"><i class="fa fa-pencil"></i></a> 
                                    <a href="/admin/blog/delete/<%= blog.id %>" class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="top" title="Delete"><i class="fa fa-trash"></i></a> 
                                </td>
                            </tr>
                        <% }); %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>